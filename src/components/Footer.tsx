import React from "react";
import { getDictionary } from "@/lib/dictionary";

type Props = {
  // navbar: LangsDict["navbar"];
};
function Footer({}: Props) {
  return <div>Footer</div>;
}

export default Footer;
